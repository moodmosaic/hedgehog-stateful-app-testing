cabal-version: >= 1.10

name: tutorial
version: 0.0.0
build-type: Simple

library
  exposed-modules:
      Tutorial

  build-depends:
      base
    , postgresql-simple
    , text
    , time

  default-language:
      Haskell2010
